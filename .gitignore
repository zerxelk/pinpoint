# Python / backend
__pycache__/
*.pyc
.venv/
venv/
.env

# Node / frontend
node_modules/
dist/
build/

# Tauri
src-tauri/target/

# Bundled PyInstaller backend binary — build it locally, don't commit it.
# See the README for the PyInstaller build + copy step.
frontend/src-tauri/binaries/mirage-backend-aarch64-apple-darwin

# macOS
.DS_Store

# Editor
.idea/
.vscode/
# SQLite DB is created at runtime next to whichever backend is running
places.db
